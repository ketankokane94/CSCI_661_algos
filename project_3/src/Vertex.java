import java.util.Comparator;

public class Vertex {
    String Name;
    Boolean isSource;
    Boolean isSink;

    @Override
    public boolean equals(Object obj) {
        return super.equals(obj);
    }
}
